---
layout: legal
title: Seitenübersicht
sitemap:
  ignore: true
---

<ul>
  {% for category in collections.allSitemapSorted %}
    <h2>{{ category | first }}</h2>
    {% for path in category[1] %}
      <li>
        <a href="{{ path }}">https://arbeitskreis.video{{ path }}</a>
      </li>
    {% endfor %}
  {% endfor %}
</ul>
