<div class="col-md-4 col-sm-6 timeline-item border border-light">
  <div class="timeline-caption">
    <p class="text-muted fs-5 mb-0">{{ timelineElem.date }}</p>
    <hr />
    <h4 class="text-primary">{{ timelineElem.title }}</h4>
    <hr />
    <p>
      {{ timelineElem.description }}
    </p>
  </div>
</div>
